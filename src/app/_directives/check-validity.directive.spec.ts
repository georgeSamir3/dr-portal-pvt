import { CheckValidityDirective } from './check-validity.directive';

describe('CheckValidityDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckValidityDirective();
    expect(directive).toBeTruthy();
  });
});
