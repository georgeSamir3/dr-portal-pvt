import { GetTestPrescriptionNamePipe } from './get-test-prescription-name.pipe';

describe('GetTestPrescriptionNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetTestPrescriptionNamePipe();
    expect(pipe).toBeTruthy();
  });
});
