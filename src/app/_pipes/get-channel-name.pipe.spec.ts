import { GetChannelNamePipe } from './get-channel-name.pipe';

describe('GetChannelNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetChannelNamePipe();
    expect(pipe).toBeTruthy();
  });
});
